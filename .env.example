# Backend API (Express on PORT, default 3000). Must include /api/v1 for axios baseURL.
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Optional: used by some UI copy / links
# VITE_FRONTEND_ORIGIN_ADMIN=http://localhost:8081
